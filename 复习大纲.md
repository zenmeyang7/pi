# 复习大纲 —— 我一个月写的 agent 从零到能跑

> 这份是**总地图**，不是代码手册。目的：扫一眼就能想起"整条线是怎么串起来的"。
> ✅ = 已完成，🔨 = 写好了但没最终验证，▢ = 还没写。

---

## 一、这个项目是什么

一个**命令行（REPL）交互的 Agent**：你输入一句话，它决定要不要用工具、算完回话，再用你说的下一句继续，直到你输 `quit`。对话历史持久化在本地 `.jsonl` 文件里，重启自动恢复，还能分叉、回溯、开多个会话。

```
用户输入 → [判定] → 问大模型 → 模型决定用工具 → 执行工具 → 结果喂回 → 模型总结 → 答完停下等你 → 循环
```

核心骨架：**终端输入(Steering) → 控制循环(agent_loop) → 调模型(StreamFn) → 到处读写的会话树(JsonlTree)**。

---

## 二、每个文件干什么

| 文件 | 职责 | 一句话 |
|---|---|---|
| `main.py` | 入口：搭工具、开会话、启动 REPL、Steering 读终端 | "外壳"，把各部分接起来 |
| `jsonl_tree.py` | `JsonlTree` 一棵会话树 + `SessionRepo` 多会话管理 | 唯一持久化层，历史+分支 |
| `agent_loop.py` | `run_loop` / `run_agent_loop` / 执行工具 | "引擎"，调度模型和工具轮次 |
| `type.py` | `AgentContext` / `AgentTool` / `AgentLoopconfig` / `convert_to_llm` / `StreamFn` | "类型&协议"，定义数据和 API |
| `context_manager.py` | token 计数 `count_token` / `count_context_tokens` | L2 的"尺子" |
| `.env` | `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` | 密钥（**绝不能提交 git**） |

数据流一句话：
**Steering 收到输入 → 交给 run_loop → run_loop 调 streamAssistantResponse（从 tree 读历史→转协议→StreamFn 调 LLM）→ 有工具调用就 executeToolCalls → 结果 append 回 tree → 循环到模型 stop → wait_for_steering 等你。**

---

## 三、核心概念（这些是灵魂，务必理解）

### 1. 会话树 JsonlTree —— 用 `parentId` 串成链
- 文件是 `.jsonl`：**一行一个 JSON**（header / entry / lane）。
- 每个 `entry` 有一个 `parentId`，指向它的上一条——这样所有消息能回溯出一条链。
- `for_path()` 从当前叶子**沿 parentId 一路走回根**，反转成时间序 = 喂给 LLM 的历史。

### 2. lane（车道/分支）—— 一套"光标"
- `lanes[lane]` = 每条车道各自**最后一条消息的 id**（叶子）。
- `active` = 我现在站在哪条车道上。
- `branch()` 从当前节点开一条新车道；`switch("main")` 切回主线；`back()` 把当前车道的叶子退回它的 parent。

### 3. 树是"事实源"（最重要的一条设计）
- **LLM 读的是 `tree.for_path()`，不是内存里的 `context.messages` 列表。**
- 所以 `/branch` `/back` `/fork` 改的是树的光标，LLM 下一次立刻看到新视野。这就是为什么"树是事实源"。

### 4. append_message：写穿（write-through）
- 先写树（事实源），再写内存列表（向后兼容回退）：
  ```python
  if self.tree is not None:
      self.tree.append(message=m)
  self.messages.append(m)
  ```

### 5. Steering：轮询 vs 阻塞
- 两个接口：`__call__`（agent 干活时**非阻塞轮询**有没有新输入）、`wait()`（agent 答完**阻塞等**你下一条）。
- **deferred 机制**：轮询时遇到 `/` 命令或 `quit` **不执行也不发 LLM**，先存起来，等 `wait()`（turn 之间）真正执行。避免在工具轮次中途分叉把 tool_results 搞丢。

### 6. 协议规则：最后一条不能是 assistant
- LLM 是"回复方"，只能回应 user 提问或 tool 结果。所以历史尾部必须是 **user 或 tool**，不能是 assistant（否则 api 报错/乱回）。`convert_to_llm_default` 只留 user/assistant/tool 三种，正是守这个交替结构。

### 7. token 计数
- token 是模型切文本的"片"，不是字数。精确用 `tiktoken` 的 `cl100k_base`。
- `count_context_tokens(system_prompt, messages, tools)` = system + 全部消息 + 工具定义一起数，代表"这次调用花了多少 token"。

---

## 四、命令清单

| 命令 | 作用 | 维度 |
|---|---|---|
| `/branch` | 从当前节点开一条**新车道** | 同一会话内 |
| `/main` | 把光标**切回主线** | 同一会话内 |
| `/back` | **当前车道光标退回上一条**，之后重新分叉 | 同一会话内 |
| `/new <名>` | 新建一个**空会话** | 跨会话 |
| `/open <名>` | 打开一个**已有会话** | 跨会话 |
| `/fork <名>` | 把**当前会话复制**成一个新会话（带历史） | 跨会话 |
| `/list` | 列出所有会话 | 跨会话 |
| `quit` / `q` / `exit` | 退出 | — |

维度总结：`/branch` `/back` `/main` 动**车道**；`/fork` `/new` `/open` 动**会话（文件）**。

---

## 五、踩过的坑（复盘最珍贵）

- **`json.load` vs `json.loads`**：`load` 读整个文件（jsonl 有好多行→崩），`loads` 读字符串。jsonl 每行要用 `json.loads(line)`。
- **`.jsonl` 不是 `.json`**：`list()` 用 `endswith(".jsonl")`，否则跳过所有文件。
- **`t["definition"]` vs `t.definition`**：AgentTool 是对象，用**点号**取属性，不是字典方括号。
- **`self.active or lane` vs `lane or self.active`**：`or` 取第一个为真的值，顺序错了参数就失效。
- **`parentId` 为空/无效要挡**：load 时 `if parentId is not None and parentId not in entries: continue`，否则孤儿节点崩。
- **Windows 环境**：用 `uv run python`（裸 `python` 是商店占位符）；`sys.stdout.reconfigure(encoding="utf-8")` 防中文乱码；`VIRTUAL_ENV` 不匹配是良性警告。
- **`.env` 必须进 `.gitignore`**：里面有 API key，不能提交。
- **旧文件格式错会毁掉 load**：早先手动写过字符串消息的 jsonl，load() 重放时会崩——删掉重来。

---

## 六、L1-L5 进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| L1 | 会话树 + 多会话 + /branch /main /fork /back /new /list /open | ✅ 完成 |
| L2-1 | token 精确计数 + 每轮打印 | ✅ 完成 |
| L2-2 | compaction：超线时老消息浓缩成摘要进 system | ▢ 接着做 |
| L2-3 | 流式输出 | ▢ |
| L3 | 真并行工具 + 读写文件工具 + hooks | ▢ |
| L4 | 记忆检索（倒排索引 + /search） | ▢ |
| L5 | 多 provider + 终端菜单 UI + README | ▢ |

---

## 七、下面的路（L2-2 compaction）

- 设红线 `MAX_TOKENS`。每次调 LLM 前 `count_context_tokens`，**超线**就把**最老一批消息**交 LLM 浓缩成一段摘要，塞进 system prompt 顶部，然后从历史**丢掉**旧消息。
- 一句话：**聊太长记不住 → 把重要的做成"小抄"贴在最前，旧的扔掉，但意思还在。**

---

## 八、建议的复习方式

1. 先看**第二节文件表**建立整体观。
2. 再死磕**第三节的 7 个概念**——这是灵魂，别的都是包装。
3. 拿一张纸默写 `for_path` 的 parentId 回溯流程 / Steering 的 deferred 流程 / 一次对话的完整数据流。
4. 用第五节的坑自测：每个坑都能说出"为什么错、怎么改、哪一行"。
5. 对照第六节进度表，确保知道**下一步**是什么。
